Bootstrap: docker
From: python:3.9-slim

%files
    receiver/requirements.txt /app/requirements.txt

%post
    pip install --no-cache-dir -r /app/requirements.txt

%environment
    export PYTHONPATH=/app

%runscript
    cd /app
    exec python -u app.py
