Bootstrap: docker
From: python:3.9-slim

%files
    edge_encoder/requirements.txt /app/requirements.txt

%post
    pip install --no-cache-dir -r /app/requirements.txt

%environment
    export PYTHONPATH=/app

%runscript
    cd /app
    exec uvicorn app:app --host 0.0.0.0 --port 8000
